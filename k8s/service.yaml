apiVersion: v1
kind: Service
metadata:
  name: baker-street
  namespace: baker-street
spec:
  type: NodePort
  selector:
    app: baker-street
  ports:
    - name: web
      port: 3000
      targetPort: 3000
    - name: mcp
      port: 3100
      targetPort: 3100
