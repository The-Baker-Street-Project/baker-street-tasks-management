# Database
DATABASE_URL=postgresql://baker:baker_dev@localhost:5432/baker_street_tasks

# MCP Server (generate a key with: openssl rand -hex 32)
MCP_API_KEY=
MCP_PORT=3100

# Web App
NEXT_PUBLIC_APP_URL=http://localhost:3000
